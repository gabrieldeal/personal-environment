#!/usr/bin/env bash
#-*- shell-script -*-

dir="/cygdrive/e"
# rsync --delete --dry-run
mkdir -p "$dir" \
    && rsync \
	   --delete-before \
	   --exclude '*/AppData/*' \
	   -avh \
	   "/cygdrive/c/Users" \
	   "$dir" \
	   "$@"
