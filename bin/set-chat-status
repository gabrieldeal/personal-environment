#!/bin/bash
#-*- shell-script -*-

STATUS=$1

set-hipchat-status -u '@GabrielDeal' \
                   -a ~/tmp/auth-token.txt \
                   -h paradem.hipchat.com \
                   -s $STATUS
