#!/usr/bin/env bash

# Can't use rsync because some files names are too long for the filesystem.

tar zcvf "/tmp/Kindle.tgz" "/media/$USER/Kindle/documents" \
    && mv "/tmp/Kindle.tgz" "$HOME/Kindle.tgz"

