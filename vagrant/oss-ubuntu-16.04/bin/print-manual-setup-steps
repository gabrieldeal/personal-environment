#!/usr/bin/env bash
# -*- shell-script -*-

echo '


If this is a brand new VM, then run this in the VM:
    ssh-agent -s >~/.ssh-agent
    eval `cat ~/.ssh-agent`
    ssh-add
    /vagrant/bin/setup-gabriel-personal-env
    /vagrant/bin/setup-huddle
'
