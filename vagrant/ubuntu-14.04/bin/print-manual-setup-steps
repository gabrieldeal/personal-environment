#!/usr/bin/env bash
# -*- shell-script -*-

echo '


If this is a brand new VM, then run this in the VM:
    passwd vagrant
    ssh-agent -s >~/.ssh-agent
    eval `cat ~/.ssh-agent`
    ssh-add
    setup-gabriel-personal-env
    ~/bin/setup-huddle
'
